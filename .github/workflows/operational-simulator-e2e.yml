name: Apply Authenticated E2E Race Fix

on:
  pull_request:
    branches: [main]
    paths:
      - "scripts/apply-auth-race-e2e-fix.py"
      - ".github/workflows/operational-simulator-e2e.yml"

permissions:
  contents: write

concurrency:
  group: apply-auth-race-e2e-fix
  cancel-in-progress: true

jobs:
  apply:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4
        with:
          ref: feature/25-30-homes-simulator-v1
          fetch-depth: 1
      - uses: pnpm/action-setup@v4
        with:
          version: 9.15.4
      - uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: pnpm
      - name: Apply authenticated E2E race fix
        run: python scripts/apply-auth-race-e2e-fix.py
      - name: Install dependencies
        run: pnpm install --frozen-lockfile
      - name: Validate production build
        run: pnpm build
      - name: Commit validated test changes
        shell: bash
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add tests/operational-simulator.spec.ts tests/canonical-route-sync.spec.ts
          git commit -m "test: stabilize authenticated route E2E requests"
          git push origin HEAD:feature/25-30-homes-simulator-v1
