name: Apply Canonical Employee Identity Repair

on:
  pull_request:
    branches: [main]
    paths:
      - "scripts/apply-canonical-employee-identity.py"
      - ".github/workflows/operational-simulator-e2e.yml"

permissions:
  contents: write

concurrency:
  group: apply-canonical-employee-identity
  cancel-in-progress: true

jobs:
  apply:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4
        with:
          ref: feature/25-30-homes-simulator-v1
          fetch-depth: 1
      - uses: pnpm/action-setup@v4
        with:
          version: 9.15.4
      - uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: pnpm
      - name: Apply canonical Employee identity repair
        run: python scripts/apply-canonical-employee-identity.py
      - name: Install dependencies
        run: pnpm install --frozen-lockfile
      - name: Validate production build
        run: pnpm build
      - name: Commit validated functional changes
        shell: bash
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add app/api/map/canonical-route/route.ts tests/operational-simulator.spec.ts
          git commit -m "fix: resolve Employee routes from canonical Visit assignments"
          git push origin HEAD:feature/25-30-homes-simulator-v1
