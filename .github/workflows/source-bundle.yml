name: Source Bundle Audit

on:
  push:
    branches:
      - feature/25-30-homes-simulator-v1
    paths:
      - .github/workflows/source-bundle.yml

permissions:
  contents: write

jobs:
  bundle:
    if: false
    runs-on: ubuntu-latest
    steps:
      - run: echo disabled while patch payload is updated
