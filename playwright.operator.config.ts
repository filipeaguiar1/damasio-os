// Browser Operator QA uses playwright.operator.config.mjs.
// This compatibility stub intentionally has no Playwright imports so the core
// application TypeScript gate does not require the temporary E2E dependency.
export {};
