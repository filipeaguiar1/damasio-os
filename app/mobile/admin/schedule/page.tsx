"use client";
import {useEffect,useMemo,useState} from "react";
import {MobileRoleGuard} from "@/components/mobile/MobileRoleGuard";
import {MobileBackButton} from "@/components/mobile/MobileBackButton";
import {MobileAdminNav} from "@/components/mobile/MobileAdminNav";
import {DAMASIO_SYNC_EVENT,getLeads,type Lead} from "@/lib/storage";
export default function MobileAdminSchedule(){const[tick,setTick]=useState(0);const[selected,setSelected]=useState<Lead|null>(null);useEffect(()=>{const on=()=>setTick(v=>v+1);window.addEventListener(DAMASIO_SYNC_EVENT,on as EventListener);window.addEventListener("storage",on);return()=>{window.removeEventListener(DAMASIO_SYNC_EVENT,on as EventListener);window.removeEventListener("storage",on)}},[]);const groups=useMemo(()=>{tick;return Object.entries(getLeads().filter(l=>l.scheduledDate||l.nextVisitDate).reduce<Record<string,Lead[]>>((a,l)=>{const d=l.scheduledDate||l.nextVisitDate||"Unscheduled";(a[d]??=[]).push(l);return a},{})).sort(([a],[b])=>a.localeCompare(b))},[tick]);return <MobileRoleGuard allowed={["admin","manager"]}><main className="mobile-app-shell role-mobile-shell mobile-native-subpage"><header className="role-mobile-topbar"><MobileBackButton fallback="/mobile/admin"/><div><strong>Schedule</strong><span>Calendar and planned visits</span></div><span className="role-mobile-avatar">□</span></header><section className="mobile-native-hero"><span>PLANNED WORK</span><h1>Visits organized by day.</h1><p>Tap a visit to see the customer, profile photo and complete property instructions.</p></section><section className="mobile-city-groups">{groups.map(([date,rows])=><section key={date}><header><h2>{new Date(`${date}T12:00:00`).toLocaleDateString("en-CA",{weekday:"long",month:"short",day:"numeric"})}</h2><span>{rows.length} visits</span></header>{rows.map(lead=><button type="button" onClick={()=>setSelected(lead)} key={lead.id}><i className="mobile-schedule-thumb">{lead.propertyPhoto?<img src={lead.propertyPhoto} alt=""/>:"□"}</i><div><strong>{lead.name}</strong><span>{lead.address}</span><small>{lead.assignedCrew||"Unassigned"} · {lead.service}</small></div><b>{lead.status==="completed"?"✓":"›"}</b></button>)}</section>)}</section>{selected&&<ScheduleSheet lead={selected} close={()=>setSelected(null)}/>}<MobileAdminNav active="home"/></main></MobileRoleGuard>}
function ScheduleSheet({lead,close}:{lead:Lead;close:()=>void}){const d=lead.propertyDetails;return <div className="mobile-native-modal mobile-detail-sheet"><button className="mobile-native-scrim" onClick={close}/><section><header><div><span>SCHEDULED PROPERTY</span><h2>{lead.name}</h2></div><button onClick={close}>×</button></header><div className="mobile-sheet-property-photo">{lead.propertyPhoto?<img src={lead.propertyPhoto} alt={`Profile of ${lead.address}`}/>:<span>⌂<small>No profile photo</small></span>}</div><dl className="mobile-property-dl"><div><dt>Address</dt><dd>{lead.address}</dd></div><div><dt>Visit</dt><dd>{lead.scheduledDate||lead.nextVisitDate}</dd></div><div><dt>Service</dt><dd>{lead.service} · {lead.serviceFrequency||"One time"}</dd></div><div><dt>Value</dt><dd>${lead.total.toFixed(2)}</dd></div><div><dt>Crew</dt><dd>{lead.assignedCrew||"Unassigned"}</dd></div><div><dt>Lawn</dt><dd>{d?.lawnSize||"Not set"} · {d?.grassHeight||"Height not set"}</dd></div><div><dt>Access</dt><dd>{d?.gated?"Gated":"Open"} · {d?.backyard?"Backyard":"No backyard"}</dd></div><div><dt>Instructions</dt><dd>{d?.accessNotes||d?.propertyAlerts||lead.notes||"No special instructions"}</dd></div><div><dt>Contact</dt><dd>{lead.phone||"No phone"} · {lead.email||"No email"}</dd></div></dl><a className="mobile-native-submit" href={`/mobile/admin/customers?property=${lead.id}`}>Open complete customer record</a></section></div>}
