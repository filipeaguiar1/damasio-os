import{AdminShell}from"@/components/admin/AdminShell";import{basePrices,memberships,serviceLabels}from"@/lib/pricing";export default function Settings(){const rows=Object.entries(basePrices).filter(([k])=>k!=="extra_service");return <AdminShell active="Settings"><div className="app-top"><div><span className="eyebrow">Configuration</span><h1>Settings</h1></div></div><div className="settings-grid"><div className="settings-nav"><a>Pricing</a><a>Taxes</a><a>Memberships</a><a>Payments</a></div><div className="card profile-card"><h2>Base Prices</h2>{rows.map(([k,v])=><div className="price-row" key={k}><strong>{serviceLabels[k as keyof typeof serviceLabels]}</strong><span>${v}</span><button className="btn btn-outline">Edit</button></div>)}<h2>Memberships</h2>{memberships.map(p=><div className="price-row" key={p.name}><strong>{p.name}</strong><span>${p.price}/mo</span><button className="btn btn-outline">Edit</button></div>)}</div></div></AdminShell>}