"use client";import{useEffect,useState}from"react";import{AdminShell}from"@/components/admin/AdminShell";import{addActivityLog,getActivityLogs,seedDemoLeads}from"@/lib/storage";export default function Logs(){const[logs,setLogs]=useState(getActivityLogs());function demo(){seedDemoLeads(true);addActivityLog("System","Demo loaded","4Ever Seasons","Demo data loaded by admin.");setLogs(getActivityLogs())}useEffect(()=>setLogs(getActivityLogs()),[]);return <AdminShell active="Logs"><div className="app-top"><div><span className="eyebrow">Audit Trail</span><h1>Activity Logs</h1></div><button className="btn btn-primary" onClick={demo}>Create Demo Log</button></div><section className="card profile-card">{logs.length===0?<p>No logs yet.</p>:logs.map(log=><div className="log-row" key={log.id}><div>{new Date(log.createdAt).toLocaleTimeString([], {hour:"2-digit", minute:"2-digit"})}</div><div><strong>{log.actor}</strong> — {log.action}<br/><small>{log.target}</small></div><div>{log.details}</div></div>)}</section></AdminShell>}